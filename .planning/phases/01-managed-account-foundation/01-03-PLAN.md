---
phase: 01-managed-account-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Sources/CodeRelayCodex/CodexHomeScope.swift
  - Sources/CodeRelayCodex/CodexLoginRunner.swift
  - Sources/CodeRelayCodex/CodexIdentityReader.swift
  - Sources/CodeRelayCodex/CredentialStoreDetector.swift
  - Sources/CodeRelayApp/AppContainer.swift
  - Sources/CodeRelayApp/Accounts/AccountsFeature.swift
  - Sources/CodeRelayApp/Accounts/AccountsView.swift
  - Tests/CodeRelayCodexTests/CredentialStoreDetectorTests.swift
  - Tests/CodeRelayAppTests/AccountsFeatureTests.swift
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
  - ACCT-03
  - ACCT-04
  - ACCT-05
  - ACCT-06
must_haves:
  truths:
    - "A managed account can be enrolled or re-authenticated through a Codex login flow scoped to a per-account managed home."
    - "The Phase 1 app surface shows every managed account with email, last authentication time, active/live state, and switching-support state."
    - "The user can set the active account, re-authenticate an existing row, and remove an account from the Phase 1 surface without touching switching or warning flows."
  artifacts:
    - path: Sources/CodeRelayCodex/CodexLoginRunner.swift
      provides: "Scoped `codex login` execution for managed homes"
    - path: Sources/CodeRelayCodex/CredentialStoreDetector.swift
      provides: "Supported / unsupported / unverified classification"
    - path: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      provides: "Phase 1 account-management state and actions"
    - path: Sources/CodeRelayApp/Accounts/AccountsView.swift
      provides: "Minimal account-management surface"
  key_links:
    - from: Sources/CodeRelayCodex/CodexLoginRunner.swift
      to: Sources/CodeRelayCodex/CodexHomeScope.swift
      via: "managed `CODEX_HOME` process environment"
      pattern: "CODEX_HOME"
    - from: Sources/CodeRelayApp/AppContainer.swift
      to: Sources/CodeRelayCore/ManagedAccountStore.swift
      via: "feature service wiring"
      pattern: "JSONManagedAccountStore"
    - from: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      to: Sources/CodeRelayCodex/CredentialStoreDetector.swift
      via: "support-state display and enrollment flow"
      pattern: "CredentialStoreDetector"
---

<objective>
Implement the Codex-scoped enrollment layer and the thin Phase 1 account-management surface.

Purpose: This is the user-facing slice that turns the Phase 1 contracts and core logic into actual managed-account workflows.
Output: Scoped login/re-auth support detection plus a minimal account-management feature covering ACCT-01 through ACCT-06.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-managed-account-foundation/01-RESEARCH.md
@.planning/phases/01-managed-account-foundation/01-VALIDATION.md
@Package.swift
@Sources/CodeRelayCore/ManagedAccount.swift
@Sources/CodeRelayCore/AccountSupportState.swift
@Sources/CodeRelayCore/CodeRelayPaths.swift
@Sources/CodeRelayCore/ManagedAccountStore.swift
@Sources/CodeRelayCore/AccountProjection.swift
@Sources/CodeRelayCodex/CodexHomeScope.swift
@Sources/CodeRelayCodex/CodexLoginRunner.swift
@Sources/CodeRelayCodex/CodexIdentityReader.swift
@Sources/CodeRelayCodex/CredentialStoreDetector.swift
@Sources/CodeRelayApp/AppContainer.swift
@Sources/CodeRelayApp/Accounts/AccountsFeature.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement scoped Codex enrollment, re-authentication, and support-state detection</name>
  <files>Sources/CodeRelayCodex/CodexHomeScope.swift, Sources/CodeRelayCodex/CodexLoginRunner.swift, Sources/CodeRelayCodex/CodexIdentityReader.swift, Sources/CodeRelayCodex/CredentialStoreDetector.swift, Tests/CodeRelayCodexTests/CredentialStoreDetectorTests.swift</files>
  <read_first>
- .planning/phases/01-managed-account-foundation/01-RESEARCH.md
- .planning/phases/01-managed-account-foundation/01-VALIDATION.md
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/CodeRelayPaths.swift
- Sources/CodeRelayCodex/CodexHomeScope.swift
- Sources/CodeRelayCodex/CodexLoginRunner.swift
- Sources/CodeRelayCodex/CodexIdentityReader.swift
- Sources/CodeRelayCodex/CredentialStoreDetector.swift
- Tests/CodeRelayCodexTests/CredentialStoreDetectorTests.swift
  </read_first>
  <behavior>
- a new account login creates or reuses `managed-codex-homes/<account-id>/` and runs `codex login` with `CODEX_HOME` pointed there
- re-authentication can target an existing account id and keep the existing managed home instead of creating a duplicate row
- credential-store classification returns only `supported`, `unsupported`, or `unverified` with an explanatory reason
  </behavior>
  <action>First replace the placeholder detector tests with concrete cases for file-backed auth, shared/keychain-like auth, and unknown/unverified auth layouts. Then implement `CodexHomeScope`, `DefaultCodexLoginRunner`, `DefaultCodexIdentityReader`, and `DefaultCredentialStoreDetector`. `DefaultCodexLoginRunner` must launch `codex login` with a scoped `CODEX_HOME` under `CodeRelayPaths.managedHomesRoot`; `DefaultCodexIdentityReader` must read identity from the managed home, not from ambient `~/.codex`; `DefaultCredentialStoreDetector` must surface `supported`, `unsupported`, or `unverified` per ACCT-06. Keep this plan pre-switch: do not touch live `~/.codex`, process restarts, warning thresholds, or Codex App validation.</action>
  <verify>
    <automated>swift test --filter CredentialStoreDetectorTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCodex/CodexLoginRunner.swift` sets `CODEX_HOME` when launching `codex login`
- `Sources/CodeRelayCodex/CredentialStoreDetector.swift` exposes only `supported`, `unsupported`, and `unverified` support states with reason text
- `Tests/CodeRelayCodexTests/CredentialStoreDetectorTests.swift` asserts file-backed, shared-store, and unknown-mode classification
- No file in this plan references `~/.codex/auth.json`, `~/.codex/config.toml`, `resume`, or restart orchestration
  </acceptance_criteria>
  <done>Scoped account enrollment and support-state classification are implemented without drifting into switch automation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Phase 1 account-management feature and app wiring</name>
  <files>Sources/CodeRelayApp/AppContainer.swift, Sources/CodeRelayApp/Accounts/AccountsFeature.swift, Sources/CodeRelayApp/Accounts/AccountsView.swift, Tests/CodeRelayAppTests/AccountsFeatureTests.swift</files>
  <read_first>
- .planning/phases/01-managed-account-foundation/01-RESEARCH.md
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/AccountProjection.swift
- Sources/CodeRelayCore/ManagedAccountStore.swift
- Sources/CodeRelayCodex/CodexLoginRunner.swift
- Sources/CodeRelayCodex/CodexIdentityReader.swift
- Sources/CodeRelayCodex/CredentialStoreDetector.swift
- Sources/CodeRelayApp/AppContainer.swift
- Sources/CodeRelayApp/Accounts/AccountsFeature.swift
- Tests/CodeRelayAppTests/AccountsFeatureTests.swift
  </read_first>
  <behavior>
- the feature lists every managed account row with email, last auth time, `isActive`, `isLive`, and support-state label
- the user can trigger add, set-active, re-authenticate, and remove actions without creating duplicate rows
- active selection persists with a single key and is reconciled if the selected account disappears
  </behavior>
  <action>Expand `Tests/CodeRelayAppTests/AccountsFeatureTests.swift` into concrete feature tests for row rendering state, set-active persistence, re-auth action routing, and guarded removal behavior. Then implement `AccountsFeature` and `AccountsView` so the Phase 1 surface shows managed account rows and actions only for ACCT-01 through ACCT-06. Wire `AppContainer` to the Phase 1 concrete services: `JSONManagedAccountStore`, `DefaultAccountProjection`, `DefaultCodexLoginRunner`, `DefaultCodexIdentityReader`, and `DefaultCredentialStoreDetector`. Persist the selected active account id with one explicit key such as `activeManagedAccountID`. Do not add usage monitoring, warning configuration, switch confirmation, restart flows, or Codex App validation UI.</action>
  <verify>
    <automated>swift test --filter AccountsFeatureTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` renders account rows containing email, last auth time, active/live markers, and support-state text
- `Sources/CodeRelayApp/Accounts/AccountsFeature.swift` defines actions for add, set-active, re-authenticate, and remove
- `Sources/CodeRelayApp/AppContainer.swift` wires the Phase 1 concrete services and stores the active account key as `activeManagedAccountID`
- No file in this plan adds warning-threshold, switching, relaunch, or resume commands
  </acceptance_criteria>
  <done>The Phase 1 user-facing slice exists and can drive managed-account enrollment and maintenance without leaving scope.</done>
</task>

</tasks>

<verification>
Run `swift test --filter CredentialStoreDetectorTests`, `swift test --filter AccountsFeatureTests`, and then `swift test` once both wave-2 plans are merged. Confirm the Phase 1 surface stays account-management-only and every visible state maps back to the domain contracts.
</verification>

<success_criteria>
Phase 1 exposes a working, scoped account-management flow that surfaces identity, active/live state, re-auth, removal, and one-click-switch support status without implementing switching itself.
</success_criteria>

<output>
After completion, create `.planning/phases/01-managed-account-foundation/01-03-SUMMARY.md`
</output>
