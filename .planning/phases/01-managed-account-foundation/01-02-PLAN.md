---
phase: 01-managed-account-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - Sources/CodeRelayCore/ManagedAccountStore.swift
  - Sources/CodeRelayCore/AccountProjection.swift
  - Sources/CodeRelayCore/ManagedHomeSafety.swift
  - Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift
  - Tests/CodeRelayCoreTests/AccountProjectionTests.swift
  - Tests/CodeRelayCoreTests/ManagedHomeSafetyTests.swift
autonomous: true
requirements:
  - ACCT-01
  - ACCT-03
  - ACCT-04
  - ACCT-05
must_haves:
  truths:
    - "Managed account records persist under CodeRelay-owned storage with versioned JSON and atomic writes."
    - "Re-authenticating an existing managed account updates the existing record instead of creating duplicates."
    - "Account removal only deletes managed homes inside the app-owned root, and active/live projection stays internally consistent."
  artifacts:
    - path: Sources/CodeRelayCore/ManagedAccountStore.swift
      provides: "JSON-backed managed account registry and re-auth update rules"
    - path: Sources/CodeRelayCore/AccountProjection.swift
      provides: "Active/live/support-state row projection logic"
    - path: Sources/CodeRelayCore/ManagedHomeSafety.swift
      provides: "Managed-home root-bounded deletion guard"
  key_links:
    - from: Sources/CodeRelayCore/ManagedAccountStore.swift
      to: Sources/CodeRelayCore/CodeRelayPaths.swift
      via: "registry path resolution"
      pattern: "managed-codex-accounts\\.json"
    - from: Sources/CodeRelayCore/AccountProjection.swift
      to: Sources/CodeRelayCore/ManagedAccountStore.swift
      via: "managed account input and active selection correction"
      pattern: "ManagedAccount"
    - from: Sources/CodeRelayCore/ManagedHomeSafety.swift
      to: Sources/CodeRelayCore/CodeRelayPaths.swift
      via: "managed home root enforcement"
      pattern: "managed-codex-homes"
---

<objective>
Implement the Phase 1 core domain: persisted managed-account inventory, re-auth deduplication, safe removal, and active/live projection logic.

Purpose: This is the trustworthy account backbone that later login and UI work will call.
Output: Working store and projection services with real tests for ACCT-01, ACCT-03, ACCT-04, and ACCT-05 behavior.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-managed-account-foundation/01-RESEARCH.md
@.planning/phases/01-managed-account-foundation/01-VALIDATION.md
@Package.swift
@Sources/CodeRelayCore/ManagedAccount.swift
@Sources/CodeRelayCore/AccountSupportState.swift
@Sources/CodeRelayCore/CodeRelayPaths.swift
@Sources/CodeRelayCore/ManagedAccountStore.swift
@Sources/CodeRelayCore/AccountProjection.swift
@Sources/CodeRelayCore/ManagedHomeSafety.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the JSON managed-account registry and re-auth update flow</name>
  <files>Sources/CodeRelayCore/ManagedAccountStore.swift, Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift</files>
  <read_first>
- .planning/phases/01-managed-account-foundation/01-RESEARCH.md
- .planning/phases/01-managed-account-foundation/01-VALIDATION.md
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/CodeRelayPaths.swift
- Sources/CodeRelayCore/ManagedAccountStore.swift
- Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift
  </read_first>
  <behavior>
- `upsertAuthenticatedAccount(existingAccountID:)` keeps one record when a user re-authenticates the same managed account
- registry writes are atomic, versioned, and use the `managed-codex-accounts.json` path from `CodeRelayPaths`
- deleting or updating an unknown account id returns a controlled failure instead of mutating another record
  </behavior>
  <action>First expand `Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift` into real assertions for round-trip persistence, unsupported future-version handling, normalized-email deduplication, explicit-id re-auth updates, and timestamp refresh. Then implement `JSONManagedAccountStore` in `Sources/CodeRelayCore/ManagedAccountStore.swift` with a versioned on-disk payload, atomic write strategy, `0600` permissions, and CRUD methods for add/list/update/remove. Match accounts by explicit `existingAccountID` first, then normalized email plus `managedHomePath` when re-authenticating per ACCT-04. Do not add any live `~/.codex` mutation or switching behavior here.</action>
  <verify>
    <automated>swift test --filter ManagedAccountStoreTests</automated>
  </verify>
  <acceptance_criteria>
- `Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift` contains assertions for round-trip, duplicate suppression, explicit-id re-auth update, and unsupported-version behavior
- `Sources/CodeRelayCore/ManagedAccountStore.swift` references `managed-codex-accounts.json` and performs atomic writes
- `Sources/CodeRelayCore/ManagedAccountStore.swift` contains a public `upsertAuthenticatedAccount` API and updates `lastAuthenticatedAt` without creating a second account row
  </acceptance_criteria>
  <done>The managed account registry is durable, deterministic, and safe for repeated add/re-auth flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement active/live projection and managed-home removal safety</name>
  <files>Sources/CodeRelayCore/AccountProjection.swift, Sources/CodeRelayCore/ManagedHomeSafety.swift, Tests/CodeRelayCoreTests/AccountProjectionTests.swift, Tests/CodeRelayCoreTests/ManagedHomeSafetyTests.swift</files>
  <read_first>
- .planning/phases/01-managed-account-foundation/01-RESEARCH.md
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/AccountSupportState.swift
- Sources/CodeRelayCore/CodeRelayPaths.swift
- Sources/CodeRelayCore/AccountProjection.swift
- Sources/CodeRelayCore/ManagedHomeSafety.swift
- Tests/CodeRelayCoreTests/AccountProjectionTests.swift
- Tests/CodeRelayCoreTests/ManagedHomeSafetyTests.swift
  </read_first>
  <behavior>
- account rows expose `isActive`, `isLive`, `supportState`, and `lastAuthenticatedAt` without inventing usage or warning data
- when the persisted active account id no longer exists, projection returns a corrected `nil` or replacement selection instead of leaving a dangling active id
- removal safety rejects any path outside `managed-codex-homes/<account-id>/`
  </behavior>
  <action>Turn `Tests/CodeRelayCoreTests/AccountProjectionTests.swift` and `Tests/CodeRelayCoreTests/ManagedHomeSafetyTests.swift` into concrete behavior tests for active/live badge rules, invalid active-selection correction, and managed-home root enforcement. Implement `DefaultAccountProjection` in `Sources/CodeRelayCore/AccountProjection.swift` so it produces row models needed by Phase 1 UI: email, auth freshness, active/live flags, and support state/reason. Implement `DefaultManagedHomeSafety` in `Sources/CodeRelayCore/ManagedHomeSafety.swift` to verify any deletion target resolves under `CodeRelayPaths.managedHomesRoot` before file removal is allowed. Keep the logic strictly Phase 1: no switch transactions, restart orchestration, or threshold state.</action>
  <verify>
    <automated>swift test --filter "AccountProjectionTests|ManagedHomeSafetyTests"</automated>
  </verify>
  <acceptance_criteria>
- `Tests/CodeRelayCoreTests/AccountProjectionTests.swift` asserts active/live/support-state projection outcomes
- `Tests/CodeRelayCoreTests/ManagedHomeSafetyTests.swift` asserts deletion is allowed only for descendants of `managed-codex-homes`
- `Sources/CodeRelayCore/AccountProjection.swift` defines row output containing `email`, `isActive`, `isLive`, `supportState`, and `lastAuthenticatedAt`
- `Sources/CodeRelayCore/ManagedHomeSafety.swift` resolves and compares canonical paths before permitting deletion
  </acceptance_criteria>
  <done>Core account projection and deletion safety are trustworthy enough for the enrollment/UI layer to consume directly.</done>
</task>

</tasks>

<verification>
Run `swift test --filter ManagedAccountStoreTests` and `swift test --filter "AccountProjectionTests|ManagedHomeSafetyTests"`. Confirm the registry persists correctly, projection stays coherent, and deletion outside the managed root is rejected.
</verification>

<success_criteria>
Phase 1 has a real core account backbone: durable registry, deduplicating re-auth flow, active/live projection, and guarded account removal.
</success_criteria>

<output>
After completion, create `.planning/phases/01-managed-account-foundation/01-02-SUMMARY.md`
</output>
