---
phase: 02.1-app-icon-multilingual-support
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Packaging/macOS/Info.plist
  - Packaging/macOS/Icon.icon
  - Packaging/macOS/Icon.icns
  - scripts/build_icon.sh
  - scripts/build_macos_release.sh
  - Sources/CodeRelayApp/Localization/CodeRelayLocalizer.swift
  - Sources/CodeRelayApp/Resources/en.lproj/Localizable.strings
  - Sources/CodeRelayApp/Resources/zh-Hans.lproj/Localizable.strings
autonomous: true
requirements:
  - BRAND-01
  - I18N-02
must_haves:
  truths:
    - "The packaged `CodeRelay.app` contains a real CodeRelay icon instead of the default executable icon."
    - "The SwiftPM app target bundles shared localized resources that work in local `swift run` flows and the manual release-packaging flow."
    - "The product name stays `CodeRelay`, and localization infrastructure exists before UI copy migration begins."
  artifacts:
    - path: scripts/build_icon.sh
      provides: "A reproducible icon build pipeline for generating `Icon.icns` from a committed source asset"
    - path: Packaging/macOS/Info.plist
      provides: "Bundle metadata that points the packaged app at the generated icon and base localization"
    - path: scripts/build_macos_release.sh
      provides: "Manual `.app` assembly that copies icon and localization resources into the final bundle"
    - path: Sources/CodeRelayApp/Localization/CodeRelayLocalizer.swift
      provides: "Centralized localized string lookup for the app target"
    - path: Sources/CodeRelayApp/Resources/en.lproj/Localizable.strings
      provides: "English baseline strings for the current app surface"
    - path: Sources/CodeRelayApp/Resources/zh-Hans.lproj/Localizable.strings
      provides: "Simplified Chinese translations for the current app surface"
  key_links:
    - from: Package.swift
      to: Sources/CodeRelayApp/Resources/en.lproj/Localizable.strings
      via: "SwiftPM resource processing and default localization"
      pattern: "defaultLocalization"
    - from: scripts/build_macos_release.sh
      to: Packaging/macOS/Info.plist
      via: "bundle assembly and icon metadata wiring"
      pattern: "CFBundleIconFile"
    - from: Sources/CodeRelayApp/Localization/CodeRelayLocalizer.swift
      to: Sources/CodeRelayApp/Accounts/AccountsView.swift
      via: "shared localized string lookup for visible app copy"
      pattern: "localized"
---

<objective>
Establish the infrastructure for Phase 02.1: a reproducible app-icon pipeline, packaged bundle wiring, and a SwiftPM-compatible localization resource foundation for the CodeRelay app target.

Purpose: The app cannot ship a real icon or bilingual UI safely until packaging knows how to carry icon/localization resources and the app has one shared place to load localized copy.
Output: A committed icon source + generated `Icon.icns`, packaged bundle wiring, and localized resource files with an app-local lookup helper.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-app-icon-multilingual-support/02.1-CONTEXT.md
@Package.swift
@Packaging/macOS/Info.plist
@scripts/build_macos_release.sh
@Sources/CodeRelayApp/CodeRelayApp.swift
@Sources/CodeRelayApp/Accounts/AccountsView.swift
@README.md
@README.zh-CN.md
@../CodexBar/docs/icon.md
@../CodexBar/Scripts/package_app.sh

<interfaces>
From `Package.swift` today:
```swift
.executableTarget(
    name: "CodeRelayApp",
    dependencies: [
        "CodeRelayCore",
        "CodeRelayCodex",
    ],
    path: "Sources/CodeRelayApp"
)
```

From `Packaging/macOS/Info.plist` today:
```xml
<key>CFBundleDisplayName</key>
<string>CodeRelay</string>
<key>CFBundleExecutable</key>
<string>CodeRelayApp</string>
<key>CFBundleIdentifier</key>
<string>io.github.shabooboo006.CodeRelay</string>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add SwiftPM localization resources and a shared app-localization helper</name>
  <files>Package.swift, Sources/CodeRelayApp/Localization/CodeRelayLocalizer.swift, Sources/CodeRelayApp/Resources/en.lproj/Localizable.strings, Sources/CodeRelayApp/Resources/zh-Hans.lproj/Localizable.strings</files>
  <read_first>
- .planning/phases/02.1-app-icon-multilingual-support/02.1-CONTEXT.md
- Package.swift
- Sources/CodeRelayApp/CodeRelayApp.swift
- Sources/CodeRelayApp/Accounts/AccountsView.swift
- README.md
- README.zh-CN.md
  </read_first>
  <behavior>
- the app target exposes one localization entry point instead of sprinkling bundle lookups across views
- English is the base locale and Simplified Chinese is the first additional locale
- localized resources remain compatible with `swift build`, `swift run`, and the existing packaging script
  </behavior>
  <action>Update `Package.swift` so `CodeRelayApp` declares `defaultLocalization: "en"` and processes a `Resources` directory. Add `Sources/CodeRelayApp/Localization/CodeRelayLocalizer.swift` to centralize localized string lookup and common formatted-copy helpers. Seed `Sources/CodeRelayApp/Resources/en.lproj/Localizable.strings` and `Sources/CodeRelayApp/Resources/zh-Hans.lproj/Localizable.strings` with keys for the current app shell, accounts screen, monitoring labels, empty states, and action/result messages that Phase 02.1 will migrate. Keep the product name `CodeRelay` literal and unchanged across locales.</action>
  <verify>
    <automated>swift build</automated>
  </verify>
  <acceptance_criteria>
- `Package.swift` contains `defaultLocalization: "en"`
- `Package.swift` contains `resources: [.process("Resources")]`
- `Sources/CodeRelayApp/Localization/CodeRelayLocalizer.swift` exists
- `Sources/CodeRelayApp/Resources/en.lproj/Localizable.strings` exists
- `Sources/CodeRelayApp/Resources/zh-Hans.lproj/Localizable.strings` exists
  </acceptance_criteria>
  <done>CodeRelay has a shared, package-compatible localization foundation ready for UI migration.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the icon source pipeline and wire the packaged app bundle to use it</name>
  <files>Packaging/macOS/Icon.icon, Packaging/macOS/Icon.icns, Packaging/macOS/Info.plist, scripts/build_icon.sh, scripts/build_macos_release.sh</files>
  <read_first>
- .planning/phases/02.1-app-icon-multilingual-support/02.1-CONTEXT.md
- Packaging/macOS/Info.plist
- scripts/build_macos_release.sh
- ../CodexBar/docs/icon.md
- ../CodexBar/Scripts/package_app.sh
  </read_first>
  <behavior>
- the repo contains a committed source asset for the CodeRelay app icon and a reproducible way to generate `Icon.icns`
- release packaging copies the generated `Icon.icns` into `CodeRelay.app/Contents/Resources`
- the packaged app bundle advertises the icon through `Info.plist`
  </behavior>
  <action>Add a committed icon source asset at `Packaging/macOS/Icon.icon` and a shell helper at `scripts/build_icon.sh` that follows the CodexBar approach for generating `Packaging/macOS/Icon.icns` without introducing an Xcode project. Update `Packaging/macOS/Info.plist` to include the icon metadata and any base-localization metadata required for the packaged app. Update `scripts/build_macos_release.sh` so it rebuilds or copies `Icon.icns` into `dist/CodeRelay.app/Contents/Resources` before signing.</action>
  <verify>
    <automated>zsh ./scripts/build_macos_release.sh</automated>
  </verify>
  <acceptance_criteria>
- `Packaging/macOS/Info.plist` contains `CFBundleIconFile`
- `scripts/build_macos_release.sh` copies `Icon.icns` into `Contents/Resources`
- `scripts/build_icon.sh` exists
- `Packaging/macOS/Icon.icns` exists after the pipeline runs
- `dist/CodeRelay.app/Contents/Resources/Icon.icns` exists after packaging
  </acceptance_criteria>
  <done>CodeRelay release artifacts now carry a real app icon through a reproducible in-repo pipeline.</done>
</task>

</tasks>

<verification>
Run `swift build` and `zsh ./scripts/build_macos_release.sh`. Confirm the app target still builds, `Icon.icns` is present in the packaged app bundle, and localization resources are bundled without requiring Xcode project generation.
</verification>

<success_criteria>
Phase 02.1 has the infrastructure needed for both a real app icon and a shared localization system, with packaging already wired to carry those resources.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-app-icon-multilingual-support/02.1-01-SUMMARY.md`
</output>
