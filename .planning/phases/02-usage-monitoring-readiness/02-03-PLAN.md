---
phase: 02-usage-monitoring-readiness
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - Sources/CodeRelayApp/AppContainer.swift
  - Sources/CodeRelayApp/Accounts/AccountsFeature.swift
  - Sources/CodeRelayApp/Accounts/AccountsView.swift
  - Tests/CodeRelayAppTests/AccountsFeatureTests.swift
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04
  - MON-05
must_haves:
  truths:
    - "The active managed account shows 5-hour usage, weekly usage, reset timing, last refresh, and source/status inside the existing accounts surface."
    - "The user can manually refresh monitoring state and see stale/error/unknown states instead of silent failure."
    - "Alternate managed accounts show readiness headroom or an honest unavailable state without introducing warning thresholds or switching."
  artifacts:
    - path: Sources/CodeRelayApp/AppContainer.swift
      provides: "Phase 2 monitoring services wired into the app container"
    - path: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      provides: "Monitoring load, refresh orchestration, and projection into the existing accounts feature"
    - path: Sources/CodeRelayApp/Accounts/AccountsView.swift
      provides: "Visible monitoring UI and manual refresh action inside the current account-management screen"
    - path: Tests/CodeRelayAppTests/AccountsFeatureTests.swift
      provides: "Feature-level coverage for cached load, refresh, and stale fallback"
  key_links:
    - from: Sources/CodeRelayApp/AppContainer.swift
      to: Sources/CodeRelayCore/ManagedAccountUsageStore.swift
      via: "usage-cache service wiring"
      pattern: "managedAccountUsageStore"
    - from: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      to: Sources/CodeRelayCodex/CodexUsageRefreshService.swift
      via: "manual refresh orchestration"
      pattern: "codexUsageRefreshService"
    - from: Sources/CodeRelayApp/Accounts/AccountsView.swift
      to: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      via: "Refresh Usage button and monitoring row rendering"
      pattern: "refreshMonitoring"
---

<objective>
Wire the Phase 2 monitoring stack into the existing account-management surface so the user can inspect active-account usage, refresh state, and alternate readiness before any warning or switch feature exists.

Purpose: This plan completes the user-visible Phase 2 slice while preserving the existing accounts surface and the strict scope boundary around monitoring-only readiness.
Output: Manual refresh, visible source/freshness state, and alternate readiness in the current accounts feature and view.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
@.planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
@Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
@Sources/CodeRelayCore/ManagedAccountUsageStore.swift
@Sources/CodeRelayCore/AccountProjection.swift
@Sources/CodeRelayCodex/CodexUsageRefreshService.swift
@Sources/CodeRelayApp/AppContainer.swift
@Sources/CodeRelayApp/Accounts/AccountsFeature.swift
@Sources/CodeRelayApp/Accounts/AccountsView.swift
@Tests/CodeRelayAppTests/AccountsFeatureTests.swift
@../CodexBar/docs/refresh-loop.md

<interfaces>
From `Sources/CodeRelayApp/AppContainer.swift` today:
```swift
public struct Services {
    public var managedAccountStore: any ManagedAccountStore
    public var accountProjection: any AccountProjection
    public var codexLoginRunner: any CodexLoginRunner
    public var codexIdentityReader: any CodexIdentityReader
    public var credentialStoreDetector: any CredentialStoreDetector
}
```

From `Sources/CodeRelayApp/Accounts/AccountsFeature.swift` today:
```swift
public enum Action: Equatable, Sendable {
    case addAccount
    case setActive(UUID)
    case reauthenticate(UUID)
    case remove(UUID)
}

public struct State: Equatable, Sendable {
    public var rows: [AccountProjectionRow]
    public var activeManagedAccountID: UUID?
    public var liveIdentity: ManagedAccountIdentity?
    public var isBusy: Bool
    public var message: String?
    public var lastAction: Action?
}
```

From Phase 2 Plan 02 output:
```swift
public protocol CodexUsageRefreshService: Sendable {
    func refresh(account: ManagedAccount, cachedSnapshot: ManagedAccountUsageSnapshot?) async -> ManagedAccountUsageRefreshResult
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire cached monitoring load and manual refresh into AccountsFeature</name>
  <files>Sources/CodeRelayApp/AppContainer.swift, Sources/CodeRelayApp/Accounts/AccountsFeature.swift, Tests/CodeRelayAppTests/AccountsFeatureTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- .planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
- ../CodexBar/docs/refresh-loop.md
- Sources/CodeRelayCore/ManagedAccountUsageStore.swift
- Sources/CodeRelayCore/AccountProjection.swift
- Sources/CodeRelayCodex/CodexUsageRefreshService.swift
- Sources/CodeRelayApp/AppContainer.swift
- Sources/CodeRelayApp/Accounts/AccountsFeature.swift
- Tests/CodeRelayAppTests/AccountsFeatureTests.swift
  </read_first>
  <behavior>
- initial load includes cached monitoring snapshots before any manual refresh runs
- manual refresh updates the active row and alternate readiness across all managed accounts
- failed refreshes preserve stale cached data and keep rows visible instead of collapsing the feature state
  </behavior>
  <action>First add failing `Phase2_...` tests to `Tests/CodeRelayAppTests/AccountsFeatureTests.swift` for cached monitoring load, a manual refresh action that visits every managed account, and stale/error fallback when one or more account refreshes fail. Then update `Sources/CodeRelayApp/AppContainer.swift` to wire two new services: `managedAccountUsageStore` and `codexUsageRefreshService`. Update `Sources/CodeRelayApp/Accounts/AccountsFeature.swift` so `Action` adds the exact case `refreshMonitoring`. During `loadInitialState()` and `refresh(liveIdentity:)`, load cached snapshots from `managedAccountUsageStore` and pass them into `AccountProjectionInput`. Add an async monitoring refresh path that lists accounts from `managedAccountStore`, processes them in deterministic email order, calls `codexUsageRefreshService.refresh(account:cachedSnapshot:)`, upserts any returned snapshot into `managedAccountUsageStore`, keeps stale cached snapshots when the refresh result says `.stale`, and then reprojects rows. Use `state.message` to surface a short refresh outcome such as all-fresh success or mixed stale/error completion, but keep the behavior Phase 2 only: no auto-refresh scheduler, no thresholds, no warnings, no switching, and no relaunch/resume flow.</action>
  <verify>
    <automated>swift test --filter Phase2_accountsFeature</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayApp/Accounts/AccountsFeature.swift` contains `case refreshMonitoring`
- `Sources/CodeRelayApp/AppContainer.swift` contains `managedAccountUsageStore`
- `Sources/CodeRelayApp/AppContainer.swift` contains `codexUsageRefreshService`
- `Sources/CodeRelayApp/Accounts/AccountsFeature.swift` contains `codexUsageRefreshService.refresh`
- `Tests/CodeRelayAppTests/AccountsFeatureTests.swift` contains `func Phase2_accountsFeature_refreshesAllManagedAccounts`
- `rg -n "warningThreshold|switchAccount|resume|relaunch" Sources/CodeRelayApp/Accounts/AccountsFeature.swift` returns no matches
  </acceptance_criteria>
  <done>The accounts feature can now load cached monitoring state, refresh every managed account on demand, and preserve truthful stale/error outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render active usage, refresh metadata, and alternate readiness in AccountsView</name>
  <files>Sources/CodeRelayApp/Accounts/AccountsView.swift, Tests/CodeRelayAppTests/AccountsFeatureTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- .planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
- Sources/CodeRelayCore/AccountProjection.swift
- Sources/CodeRelayApp/Accounts/AccountsFeature.swift
- Sources/CodeRelayApp/Accounts/AccountsView.swift
- Tests/CodeRelayAppTests/AccountsFeatureTests.swift
  </read_first>
  <behavior>
- the active row shows 5-hour usage, weekly usage, reset timing, last refresh, source, and status
- alternate rows show readiness headroom or honest unknown/stale/error state
- manual refresh is visible in the current account-management surface and does not expose Phase 3 or Phase 4 controls
  </behavior>
  <action>Update `Sources/CodeRelayApp/Accounts/AccountsView.swift` so the existing account-management screen adds a `Button("Refresh Usage")` beside `Add Account`. Render monitoring details inside each row using these exact visible prefixes: `5-hour usage:`, `Weekly usage:`, `Last refreshed:`, `Source:`, `Status:`, and `Readiness:`. For the active row, show both usage windows plus reset timing when `resetsAt` or `resetDescription` exists. For alternate rows, show remaining headroom from `alternateReadiness` or the exact fallback labels `unknown`, `stale`, or `error` derived from the typed status. Extend `Tests/CodeRelayAppTests/AccountsFeatureTests.swift` with `Phase2` assertions that projected stale or unknown monitoring state remains visible after refresh and that the feature exposes monitoring data needed by the view. Do not add threshold controls, warning copy, switch CTAs, or relaunch/resume controls.</action>
  <verify>
    <automated>swift test --filter Phase2_accountsFeature</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains `Button("Refresh Usage")`
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains `Text("5-hour usage:`
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains `Text("Weekly usage:`
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains `Text("Last refreshed:`
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains `Text("Source:`
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains `Text("Status:`
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains `Text("Readiness:`
- `rg -n "threshold|warning|resume|relaunch|switch" Sources/CodeRelayApp/Accounts/AccountsView.swift` returns no matches
  </acceptance_criteria>
  <done>The user-visible accounts screen exposes trustworthy monitoring and readiness state without leaking into later warning or switching features.</done>
</task>

</tasks>

<verification>
Run `swift test --filter Phase2_accountsFeature` and then `swift test`. Confirm the feature loads cached monitoring data, manual refresh updates active and alternate rows, and stale/error/unknown states remain visible without introducing warning or switching behavior.
</verification>

<success_criteria>
The existing accounts surface now shows trustworthy active-account monitoring and alternate readiness, with a manual refresh path and explicit source/freshness state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usage-monitoring-readiness/02-03-SUMMARY.md`
</output>
