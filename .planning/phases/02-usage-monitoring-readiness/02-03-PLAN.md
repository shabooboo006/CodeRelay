---
phase: 02-usage-monitoring-readiness
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - Sources/CodeRelayApp/AppContainer.swift
  - Sources/CodeRelayApp/Accounts/AccountsFeature.swift
  - Sources/CodeRelayApp/Accounts/AccountsView.swift
  - Tests/CodeRelayAppTests/AccountsFeatureTests.swift
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04
  - MON-05
must_haves:
  truths:
    - "The active managed account shows 5-hour usage, weekly usage, last refresh time, and source/status information in the app surface."
    - "The user can manually refresh usage and see stale, error, or unknown states instead of silent failure."
    - "Alternate managed accounts expose readiness summaries that later phases can rely on without already introducing warnings or switching."
  artifacts:
    - path: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      provides: "Refresh orchestration and projected monitoring state for active and alternate accounts"
    - path: Sources/CodeRelayApp/Accounts/AccountsView.swift
      provides: "Phase 2 monitoring UI inside the existing account-management surface"
    - path: Tests/CodeRelayAppTests/AccountsFeatureTests.swift
      provides: "Feature-level monitoring and manual-refresh coverage"
  key_links:
    - from: Sources/CodeRelayApp/AppContainer.swift
      to: Sources/CodeRelayCodex/CodexUsageRefreshService.swift
      via: "service wiring for usage refresh"
      pattern: "CodexUsageRefreshService"
    - from: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      to: Sources/CodeRelayCore/ManagedAccountUsageStore.swift
      via: "cached usage load and refresh persistence"
      pattern: "ManagedAccountUsageStore"
    - from: Sources/CodeRelayApp/Accounts/AccountsFeature.swift
      to: Sources/CodeRelayCore/AccountProjection.swift
      via: "row projection with monitoring state"
      pattern: "AccountProjection"
---

<objective>
Integrate Phase 2 monitoring into the existing account-management feature so the user can inspect active-account usage, refresh state, and alternate readiness.

Purpose: This completes the user-visible Phase 2 slice without leaking into Phase 3 warnings or Phase 4 switching.
Output: Manual refresh, visible freshness/source state, and alternate readiness inside the Phase 1 account surface.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
@.planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
@Sources/CodeRelayCore/UsageSnapshot.swift
@Sources/CodeRelayCore/ManagedAccountUsageStore.swift
@Sources/CodeRelayCore/AccountProjection.swift
@Sources/CodeRelayCodex/CodexUsageRefreshService.swift
@Sources/CodeRelayApp/AppContainer.swift
@Sources/CodeRelayApp/Accounts/AccountsFeature.swift
@Sources/CodeRelayApp/Accounts/AccountsView.swift
@Tests/CodeRelayAppTests/AccountsFeatureTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire usage refresh and cache persistence into AccountsFeature</name>
  <files>Sources/CodeRelayApp/AppContainer.swift, Sources/CodeRelayApp/Accounts/AccountsFeature.swift, Tests/CodeRelayAppTests/AccountsFeatureTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- Sources/CodeRelayCore/ManagedAccountUsageStore.swift
- Sources/CodeRelayCore/AccountProjection.swift
- Sources/CodeRelayCodex/CodexUsageRefreshService.swift
- Sources/CodeRelayApp/AppContainer.swift
- Sources/CodeRelayApp/Accounts/AccountsFeature.swift
- Tests/CodeRelayAppTests/AccountsFeatureTests.swift
  </read_first>
  <behavior>
- startup loads cached usage snapshots before a fresh manual refresh happens
- manual refresh updates the active row and alternate readiness summaries across all managed accounts
- failed refresh preserves last-known data and surfaces stale/error state in feature state instead of dropping rows
  </behavior>
  <action>Expand `Tests/CodeRelayAppTests/AccountsFeatureTests.swift` with `Phase2` coverage for cached snapshot load, manual refresh routing, stale/error fallback, and alternate readiness projection. Then update `Sources/CodeRelayApp/AppContainer.swift` to wire the Phase 2 concrete services, including the new managed-account usage store and refresh service. Update `Sources/CodeRelayApp/Accounts/AccountsFeature.swift` to add an explicit refresh action, load cached usage snapshots during initial state refresh, batch refresh managed accounts through the refresh service, persist fresh results into the usage store, and continue projecting rows even when some account refreshes fail. Keep the behavior Phase 2 only: no warning thresholds, no auto-refresh scheduler, no switch confirmation, and no relaunch or resume flow.</action>
  <verify>
    <automated>swift test --filter AccountsFeatureTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayApp/Accounts/AccountsFeature.swift` defines a refresh-oriented action in addition to the existing Phase 1 account actions
- `Sources/CodeRelayApp/AppContainer.swift` wires a managed-account usage store and Codex usage refresh service
- `Tests/CodeRelayAppTests/AccountsFeatureTests.swift` contains `Phase2` tests for manual refresh and stale/error fallback
- No file in this task contains `warningThreshold`, `switchAccount`, `resume`, or `relaunch`
  </acceptance_criteria>
  <done>The feature layer can now load cached monitoring state and refresh managed-account usage on demand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface active usage, refresh metadata, and alternate readiness in AccountsView</name>
  <files>Sources/CodeRelayApp/Accounts/AccountsView.swift, Tests/CodeRelayAppTests/AccountsFeatureTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- Sources/CodeRelayCore/AccountProjection.swift
- Sources/CodeRelayApp/Accounts/AccountsFeature.swift
- Sources/CodeRelayApp/Accounts/AccountsView.swift
- Tests/CodeRelayAppTests/AccountsFeatureTests.swift
  </read_first>
  <behavior>
- the active row shows 5-hour usage, weekly usage, last refresh time, and source/status text
- alternate rows show readiness as remaining headroom or honest unknown/stale/error status
- manual refresh is visible and usable from the existing account-management surface without exposing Phase 3 warning controls
  </behavior>
  <action>Update `Sources/CodeRelayApp/Accounts/AccountsView.swift` so the existing account-management surface renders Phase 2 monitoring details inside each account row: a 5-hour usage summary, a weekly usage summary, last refresh copy, source/status copy, and alternate readiness copy for non-active rows. Add a manual refresh button that triggers the new feature refresh action and disable it only while the feature is busy. Extend `Tests/CodeRelayAppTests/AccountsFeatureTests.swift` or companion app tests so `Phase2` assertions prove the projected state includes active usage windows, alternate readiness summaries, and stale/error metadata. Keep the UI scoped to monitoring readiness only; do not add warning thresholds, warning banners, switch CTAs, or restart/resume controls.</action>
  <verify>
    <automated>swift test --filter AccountsFeatureTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains visible labels for 5-hour usage and weekly usage
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` contains a refresh action or button
- at least one `Phase2` test asserts projected stale or unknown monitoring state is preserved
- `Sources/CodeRelayApp/Accounts/AccountsView.swift` does not contain `threshold`, `switch`, `resume`, `warning`, or `relaunch`
  </acceptance_criteria>
  <done>The user-visible Phase 2 monitoring surface exists and remains cleanly separated from later warning/switch flows.</done>
</task>

</tasks>

<verification>
Run `swift test --filter AccountsFeatureTests` and then `swift test`. Confirm the feature loads cached monitoring data, manual refresh updates rows, and stale/error states remain visible without introducing warning or switching behavior.
</verification>

<success_criteria>
Phase 2 exposes trustworthy usage monitoring and alternate-account readiness in the app surface, with manual refresh and explicit freshness/source state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usage-monitoring-readiness/02-03-SUMMARY.md`
</output>
