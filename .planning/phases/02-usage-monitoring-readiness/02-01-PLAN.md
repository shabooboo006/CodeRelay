---
phase: 02-usage-monitoring-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
  - Sources/CodeRelayCore/ManagedAccountUsageStore.swift
  - Sources/CodeRelayCore/AccountProjection.swift
  - Sources/CodeRelayCore/CodeRelayPaths.swift
  - Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift
  - Tests/CodeRelayCoreTests/AccountProjectionTests.swift
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-05
must_haves:
  truths:
    - "CodeRelay has typed monitoring models for the active account's 5-hour usage, weekly usage, refresh timestamp, and source/status."
    - "Managed-account monitoring snapshots persist in CodeRelay-owned storage and survive relaunch."
    - "Every managed account can project readiness headroom or an honest unknown/stale/error state without introducing warnings or switch actions."
  artifacts:
    - path: Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
      provides: "Managed-account monitoring snapshot, rate-window, source, status, and readiness types"
    - path: Sources/CodeRelayCore/ManagedAccountUsageStore.swift
      provides: "Versioned JSON persistence for per-account monitoring snapshots"
    - path: Sources/CodeRelayCore/AccountProjection.swift
      provides: "Account rows enriched with active usage metadata and alternate readiness"
  key_links:
    - from: Sources/CodeRelayCore/ManagedAccountUsageStore.swift
      to: Sources/CodeRelayCore/CodeRelayPaths.swift
      via: "usage cache path resolution"
      pattern: "managedAccountUsageStoreURL"
    - from: Sources/CodeRelayCore/AccountProjection.swift
      to: Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
      via: "typed monitoring and readiness projection"
      pattern: "ManagedAccountUsageSnapshot"
    - from: Sources/CodeRelayCore/AccountProjection.swift
      to: Sources/CodeRelayCore/ManagedAccount.swift
      via: "managed account identity to readiness row mapping"
      pattern: "ManagedAccount"
---

<objective>
Establish the Phase 2 monitoring domain in `CodeRelayCore`: normalized 5-hour and weekly usage windows, persisted snapshot caching, and readiness-aware account projection.

Purpose: Phase 2 cannot add trustworthy monitoring until the core layer has one typed source of truth for usage windows, refresh timestamps, and honest unknown/stale/error states.
Output: Buildable core monitoring types, a file-backed snapshot cache, and projection support for active-account usage plus alternate-account readiness.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
@.planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
@Sources/CodeRelayCore/ManagedAccount.swift
@Sources/CodeRelayCore/CodeRelayPaths.swift
@Sources/CodeRelayCore/AccountProjection.swift
@Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift
@Tests/CodeRelayCoreTests/AccountProjectionTests.swift
@../CodexBar/docs/codex.md
@../CodexBar/Sources/CodexBarCore/UsageFetcher.swift

<interfaces>
From `Sources/CodeRelayCore/ManagedAccount.swift`:
```swift
public struct ManagedAccount: Codable, Equatable, Identifiable, Sendable {
    public let id: UUID
    public var email: String
    public var managedHomePath: String
    public var lastAuthenticatedAt: Date?
    public var switchSupport: AccountSupportState
}
```

From `Sources/CodeRelayCore/CodeRelayPaths.swift`:
```swift
public struct CodeRelayPaths: Equatable, Sendable {
    public var managedAccountsStoreURL: URL { get }
    public var managedHomesRoot: URL { get }
}
```

From `Sources/CodeRelayCore/AccountProjection.swift` today:
```swift
public struct AccountProjectionInput: Equatable, Sendable {
    public var accounts: [ManagedAccount]
    public var activeManagedAccountID: UUID?
    public var liveIdentity: ManagedAccountIdentity?
}

public struct AccountProjectionRow: Equatable, Identifiable, Sendable {
    public let id: UUID
    public var email: String
    public var isActive: Bool
    public var isLive: Bool
    public var supportState: AccountSupportState
    public var lastAuthenticatedAt: Date?
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the normalized monitoring snapshot types and persisted snapshot cache</name>
  <files>Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift, Sources/CodeRelayCore/ManagedAccountUsageStore.swift, Sources/CodeRelayCore/CodeRelayPaths.swift, Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- .planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
- ../CodexBar/docs/codex.md
- ../CodexBar/Sources/CodexBarCore/UsageFetcher.swift
- Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
- Sources/CodeRelayCore/ManagedAccountUsageStore.swift
- Sources/CodeRelayCore/CodeRelayPaths.swift
- Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift
- Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift
  </read_first>
  <behavior>
- the persisted cache stores one monitoring snapshot per managed account with 5-hour usage, weekly usage, refresh timestamp, source, status, and failure metadata
- the cache survives relaunch and returns the last known snapshot without probing the network
- updating one account never overwrites another account's cached monitoring data
  </behavior>
  <action>First add failing `Phase2_...` tests to `Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift` for round-trip persistence, version rejection, replace-by-account-id semantics, and missing-account reads returning `nil`. Then create `Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift` with these exact public symbols: `RateWindow`, `UsageProbeSource`, `UsageProbeStatus`, `ManagedAccountUsageSnapshot`, and `AlternateAccountReadiness`. Use these exact fields: `RateWindow.usedPercent: Double`, `RateWindow.windowMinutes: Int?`, `RateWindow.resetsAt: Date?`, `RateWindow.resetDescription: String?`, computed `RateWindow.remainingPercent`, `ManagedAccountUsageSnapshot.accountID: UUID`, `ManagedAccountUsageSnapshot.fiveHourWindow: RateWindow?`, `ManagedAccountUsageSnapshot.weeklyWindow: RateWindow?`, `ManagedAccountUsageSnapshot.updatedAt: Date`, `ManagedAccountUsageSnapshot.source: UsageProbeSource`, `ManagedAccountUsageSnapshot.status: UsageProbeStatus`, and `ManagedAccountUsageSnapshot.lastErrorDescription: String?`. Define `UsageProbeSource` with exactly `managedHomeOAuth`, `cache`, and `unknown`. Define `UsageProbeStatus` with exactly `fresh`, `stale`, `error`, and `unknown`. Create `Sources/CodeRelayCore/ManagedAccountUsageStore.swift` with a versioned JSON payload and a concrete `JSONManagedAccountUsageStore` that exposes `listSnapshots()`, `snapshot(for accountID: UUID)`, `upsert(_ snapshot: ManagedAccountUsageSnapshot)`, and `removeSnapshot(for accountID: UUID)`. Extend `Sources/CodeRelayCore/CodeRelayPaths.swift` with the exact filename constant `managed-account-usage.json` and a `managedAccountUsageStoreURL` property. Do not add warning thresholds, switch support, or UI copy in this task.</action>
  <verify>
    <automated>swift test --filter Phase2_managedAccountUsageStore</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift` contains `public enum UsageProbeSource: String, Codable`
- `Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift` contains `case managedHomeOAuth`, `case cache`, and `case unknown`
- `Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift` contains `public enum UsageProbeStatus: String, Codable`
- `Sources/CodeRelayCore/CodeRelayPaths.swift` contains `managed-account-usage.json`
- `Sources/CodeRelayCore/ManagedAccountUsageStore.swift` contains `public struct JSONManagedAccountUsageStore`
- `Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift` contains `func Phase2_managedAccountUsageStore_roundTripsSnapshots`
  </acceptance_criteria>
  <done>CodeRelay has a durable, typed monitoring snapshot cache that can survive relaunch and preserve truthful stale/error metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend account projection with active usage metadata and alternate readiness</name>
  <files>Sources/CodeRelayCore/AccountProjection.swift, Tests/CodeRelayCoreTests/AccountProjectionTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- ../CodexBar/Sources/CodexBarCore/UsageFetcher.swift
- Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
- Sources/CodeRelayCore/AccountProjection.swift
- Tests/CodeRelayCoreTests/AccountProjectionTests.swift
  </read_first>
  <behavior>
- projected rows expose the active account's latest 5-hour usage, weekly usage, refresh time, source, and status when cached monitoring exists
- projected rows expose alternate readiness summaries for non-active managed accounts
- accounts with no cached monitoring stay honest by surfacing `unknown` status instead of invented percentages
  </behavior>
  <action>First add failing `Phase2_...` tests to `Tests/CodeRelayCoreTests/AccountProjectionTests.swift` for active-row usage windows, alternate readiness summaries, `unknown` projection when a snapshot is missing, and preserved Phase 1 active/live correction behavior. Then update `Sources/CodeRelayCore/AccountProjection.swift` so `AccountProjectionInput` accepts `usageSnapshots: [UUID: ManagedAccountUsageSnapshot]`. Extend `AccountProjectionRow` with these exact fields: `fiveHourWindow: RateWindow?`, `weeklyWindow: RateWindow?`, `lastUsageRefreshAt: Date?`, `usageSource: UsageProbeSource`, `usageStatus: UsageProbeStatus`, `usageErrorDescription: String?`, and `alternateReadiness: AlternateAccountReadiness?`. In `DefaultAccountProjection.project`, attach monitoring data by `ManagedAccount.id`, set `alternateReadiness` only for non-active rows, keep sorting by `email`, and preserve the existing dangling-active correction semantics from Phase 1. Keep projection typed and domain-only: do not format strings, add warnings, or add switch recommendations.</action>
  <verify>
    <automated>swift test --filter Phase2_accountProjection</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCore/AccountProjection.swift` contains `public var usageSnapshots: [UUID: ManagedAccountUsageSnapshot]`
- `Sources/CodeRelayCore/AccountProjection.swift` contains `public var fiveHourWindow: RateWindow?`
- `Sources/CodeRelayCore/AccountProjection.swift` contains `public var weeklyWindow: RateWindow?`
- `Sources/CodeRelayCore/AccountProjection.swift` contains `public var usageStatus: UsageProbeStatus`
- `Sources/CodeRelayCore/AccountProjection.swift` contains `public var alternateReadiness: AlternateAccountReadiness?`
- `Tests/CodeRelayCoreTests/AccountProjectionTests.swift` contains `func Phase2_accountProjection_exposesActiveUsageWindows`
- `rg -n "warningThreshold|switch|resume|relaunch" Sources/CodeRelayCore/AccountProjection.swift` returns no matches
  </acceptance_criteria>
  <done>Core account rows can now carry truthful monitoring state for the active account and readiness summaries for alternates without leaking UI or later-phase behavior into the domain layer.</done>
</task>

</tasks>

<verification>
Run `swift test --filter Phase2_managedAccountUsageStore` and `swift test --filter Phase2_accountProjection`. Confirm the cache persists by account id, the projection attaches monitoring metadata, and missing snapshots surface `unknown` instead of fake readiness.
</verification>

<success_criteria>
Phase 2 has a stable core monitoring domain for 5-hour usage, weekly usage, refresh timestamps, source/status truth, and alternate-account readiness, all backed by persistence and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usage-monitoring-readiness/02-01-SUMMARY.md`
</output>
