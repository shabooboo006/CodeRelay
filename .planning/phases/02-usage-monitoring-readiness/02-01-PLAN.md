---
phase: 02-usage-monitoring-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/CodeRelayCore/UsageSnapshot.swift
  - Sources/CodeRelayCore/ManagedAccountUsageStore.swift
  - Sources/CodeRelayCore/AccountProjection.swift
  - Sources/CodeRelayCore/CodeRelayPaths.swift
  - Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift
  - Tests/CodeRelayCoreTests/AccountProjectionTests.swift
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-05
must_haves:
  truths:
    - "CodeRelay has a normalized usage snapshot model for 5-hour and weekly windows with explicit source and freshness state."
    - "Usage snapshots persist in a CodeRelay-owned cache instead of living only in memory."
    - "Account projection can surface alternate-account readiness or honest unknown state without inventing warnings or switch actions."
  artifacts:
    - path: Sources/CodeRelayCore/UsageSnapshot.swift
      provides: "Typed usage windows, probe source/status, and managed-account usage snapshot models"
    - path: Sources/CodeRelayCore/ManagedAccountUsageStore.swift
      provides: "Versioned persisted usage snapshot cache keyed by managed account id"
    - path: Sources/CodeRelayCore/AccountProjection.swift
      provides: "Account rows enriched with readiness and latest usage metadata"
  key_links:
    - from: Sources/CodeRelayCore/ManagedAccountUsageStore.swift
      to: Sources/CodeRelayCore/CodeRelayPaths.swift
      via: "usage cache path resolution"
      pattern: "managed-account-usage"
    - from: Sources/CodeRelayCore/AccountProjection.swift
      to: Sources/CodeRelayCore/UsageSnapshot.swift
      via: "row readiness and refresh metadata"
      pattern: "ManagedAccountUsageSnapshot"
    - from: Sources/CodeRelayCore/AccountProjection.swift
      to: Sources/CodeRelayCore/ManagedAccount.swift
      via: "managed account identity plus usage readiness projection"
      pattern: "ManagedAccount"
---

<objective>
Establish the Phase 2 core domain: typed usage windows, persisted usage snapshots, and account-row readiness projection.

Purpose: Every later Phase 2 plan needs a stable representation of 5-hour usage, weekly usage, freshness, and alternate readiness before any network probe or UI refresh flow exists.
Output: Buildable core snapshot models, a file-backed usage cache, and projection support for alternate-account readiness.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
@.planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
@Sources/CodeRelayCore/ManagedAccount.swift
@Sources/CodeRelayCore/CodeRelayPaths.swift
@Sources/CodeRelayCore/AccountProjection.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalized usage snapshot models and the persisted usage cache</name>
  <files>Sources/CodeRelayCore/UsageSnapshot.swift, Sources/CodeRelayCore/ManagedAccountUsageStore.swift, Sources/CodeRelayCore/CodeRelayPaths.swift, Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- .planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/CodeRelayPaths.swift
- Tests/CodeRelayCoreTests/ManagedAccountStoreTests.swift
  </read_first>
  <behavior>
- usage snapshots persist one record per managed account with 5-hour and weekly windows, updated timestamp, source label, and probe status
- the cache survives relaunch and returns last-known snapshots even before a fresh probe runs
- unsupported or failed probes do not mutate another account's cached snapshot
  </behavior>
  <action>First create `Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift` with `Phase2`-prefixed tests covering round-trip persistence, version checking, replace-by-account-id semantics, and unknown-account reads returning `nil` instead of throwing. Then add `Sources/CodeRelayCore/UsageSnapshot.swift` with concrete types `RateWindow`, `UsageProbeSource`, `UsageProbeStatus`, `ManagedAccountUsageSnapshot`, and `AlternateAccountReadiness`. Add `Sources/CodeRelayCore/ManagedAccountUsageStore.swift` with a versioned JSON payload rooted under `CodeRelayPaths`, using an exact cache filename that contains `managed-account-usage`, atomic writes, and CRUD APIs for list/read/upsert/remove by `UUID`. Update `Sources/CodeRelayCore/CodeRelayPaths.swift` to expose the usage-cache location without adding database dependencies or warning/switch state.</action>
  <verify>
    <automated>swift test --filter ManagedAccountUsageStoreTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCore/UsageSnapshot.swift` defines `RateWindow`, `UsageProbeSource`, `UsageProbeStatus`, `ManagedAccountUsageSnapshot`, and `AlternateAccountReadiness`
- `Sources/CodeRelayCore/ManagedAccountUsageStore.swift` contains a public store API with `UUID`-keyed read/upsert behavior
- `Sources/CodeRelayCore/CodeRelayPaths.swift` contains a path or filename string with `managed-account-usage`
- `Tests/CodeRelayCoreTests/ManagedAccountUsageStoreTests.swift` contains test names prefixed with `Phase2`
  </acceptance_criteria>
  <done>Phase 2 has a durable, testable source of truth for usage snapshots and alternate readiness metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend account projection with active usage and alternate-account readiness</name>
  <files>Sources/CodeRelayCore/AccountProjection.swift, Tests/CodeRelayCoreTests/AccountProjectionTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/UsageSnapshot.swift
- Sources/CodeRelayCore/AccountProjection.swift
- Tests/CodeRelayCoreTests/AccountProjectionTests.swift
  </read_first>
  <behavior>
- projected rows expose the active account's latest 5-hour and weekly usage when cached data exists
- projected rows expose alternate-account readiness summaries for non-active managed accounts
- rows with no cached usage remain honest by surfacing `unknown` status instead of fabricated percentages
  </behavior>
  <action>Expand `Tests/CodeRelayCoreTests/AccountProjectionTests.swift` with `Phase2` assertions for active-row usage metadata, alternate readiness summaries, stale or unknown status projection, and preserved Phase 1 active/live correction behavior. Then update `Sources/CodeRelayCore/AccountProjection.swift` so `AccountProjectionInput` accepts cached usage snapshots keyed by managed account id and `AccountProjectionRow` includes exact fields for primary 5-hour window, weekly window, last usage refresh time, usage source, usage status, and alternate readiness summary. Keep projection purely typed and domain-level: do not add UI copy strings, threshold warnings, or switch recommendation logic.</action>
  <verify>
    <automated>swift test --filter AccountProjectionTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCore/AccountProjection.swift` contains `usageStatus`, `usageSource`, and `lastUsageRefreshAt`
- `Sources/CodeRelayCore/AccountProjection.swift` references `AlternateAccountReadiness` or equivalent typed readiness summary
- `Tests/CodeRelayCoreTests/AccountProjectionTests.swift` contains `Phase2` tests for cached usage projection and unknown readiness
- No file in this plan contains `warningThreshold`, `switch`, `resume`, or `relaunch`
  </acceptance_criteria>
  <done>Core account rows can now carry trustworthy usage/readiness truth without forcing the app layer to invent monitoring logic.</done>
</task>

</tasks>

<verification>
Run `swift test --filter ManagedAccountUsageStoreTests` and `swift test --filter AccountProjectionTests`. Confirm usage snapshots persist, projection exposes typed usage metadata, and rows fall back to `unknown` rather than fake readiness when no snapshot exists.
</verification>

<success_criteria>
Phase 2 has stable core models for 5-hour usage, weekly usage, refresh freshness, and alternate-account readiness, all backed by persistent storage and projection tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usage-monitoring-readiness/02-01-SUMMARY.md`
</output>
