---
phase: 02-usage-monitoring-readiness
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - Sources/CodeRelayCodex/CodexUsageFetcher.swift
  - Sources/CodeRelayCodex/CodexUsageRefreshService.swift
  - Sources/CodeRelayCodex/CodexIdentityReader.swift
  - Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04
  - MON-05
must_haves:
  truths:
    - "CodeRelay can fetch active-account and alternate-account usage from a managed Codex home without reading ambient `~/.codex`."
    - "Every probe result carries explicit source, freshness, and failure metadata instead of silently clearing the UI."
    - "The probing layer stays Codex-only and does not import dashboard scraping, user scripts, or multi-provider source pickers."
  artifacts:
    - path: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      provides: "Managed-home scoped Codex usage request and normalization"
    - path: Sources/CodeRelayCodex/CodexUsageRefreshService.swift
      provides: "Typed single-account refresh result with status/source/error metadata"
    - path: Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift
      provides: "Probe, normalization, and degraded-state coverage"
  key_links:
    - from: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      to: Sources/CodeRelayCodex/CodexHomeScope.swift
      via: "managed-home file resolution"
      pattern: "CodexHomeScope"
    - from: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      to: Sources/CodeRelayCore/UsageSnapshot.swift
      via: "normalized usage snapshot output"
      pattern: "ManagedAccountUsageSnapshot"
    - from: Sources/CodeRelayCodex/CodexUsageRefreshService.swift
      to: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      via: "refresh orchestration and degraded-state mapping"
      pattern: "CodexUsageFetcher"
---

<objective>
Implement the Codex-specific usage probing layer that refreshes one managed account into a typed snapshot with explicit source and status metadata.

Purpose: This plan turns Phase 2 from pure domain modeling into real managed-home usage reads while preserving the narrow Codex-only scope.
Output: Testable Codex usage fetch and refresh services that the app layer can call for active and alternate accounts.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
@.planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
@Sources/CodeRelayCore/UsageSnapshot.swift
@Sources/CodeRelayCore/ManagedAccount.swift
@Sources/CodeRelayCodex/CodexHomeScope.swift
@Sources/CodeRelayCodex/CodexIdentityReader.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the managed-home Codex usage fetcher and snapshot normalization</name>
  <files>Sources/CodeRelayCodex/CodexUsageFetcher.swift, Sources/CodeRelayCodex/CodexIdentityReader.swift, Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- Sources/CodeRelayCore/UsageSnapshot.swift
- Sources/CodeRelayCodex/CodexHomeScope.swift
- Sources/CodeRelayCodex/CodexIdentityReader.swift
- Tests/CodeRelayCodexTests/CredentialStoreDetectorTests.swift
  </read_first>
  <behavior>
- usage fetch reads credentials from the managed account's home and normalizes one 5-hour window plus one weekly window
- the fetcher does not inspect ambient `~/.codex` or rely on WebKit/dashboard scraping
- failed normalization returns a typed error path instead of partial garbage values
  </behavior>
  <action>First create `Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift` with `Phase2` tests for request construction, managed-home credential resolution, successful snapshot normalization, and invalid or missing credential/error behavior. Then add `Sources/CodeRelayCodex/CodexUsageFetcher.swift` with a `CodexUsageFetcher` protocol and one default concrete type that reads managed-home auth data, performs one URLSession-based usage request through an injectable transport, and returns a normalized `ManagedAccountUsageSnapshot` for the exact managed account id being refreshed. Update `Sources/CodeRelayCodex/CodexIdentityReader.swift` only if needed to share managed-home token or identity parsing helpers; do not broaden it into an ambient account observer.</action>
  <verify>
    <automated>swift test --filter CodexUsageFetcherTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCodex/CodexUsageFetcher.swift` exports a `CodexUsageFetcher` protocol and a default implementation
- `Sources/CodeRelayCodex/CodexUsageFetcher.swift` references managed-home auth data and `ManagedAccountUsageSnapshot`
- `Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift` contains `Phase2` tests for success and failure paths
- No file in this task contains `WKWebView`, `Cookie`, `dashboard`, or `usageScript`
  </acceptance_criteria>
  <done>CodeRelay has one trustworthy Codex-only usage probe for managed accounts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement a typed refresh service with honest source and failure reporting</name>
  <files>Sources/CodeRelayCodex/CodexUsageRefreshService.swift, Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- Sources/CodeRelayCore/UsageSnapshot.swift
- Sources/CodeRelayCodex/CodexUsageFetcher.swift
- Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift
  </read_first>
  <behavior>
- a refresh result tells callers whether the snapshot is `fresh`, `error`, or `unknown`, and includes source and failure details
- missing credentials or unsupported account state produce honest `unknown` or `error` metadata instead of throwing the entire app flow off-course
- the service returns typed data for one account at a time so higher layers can batch refresh managed accounts deterministically
  </behavior>
  <action>Extend `Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift` or add companion tests so `Phase2` coverage includes missing-token behavior, transient transport failure mapping, and refresh results carrying `source`, `status`, and failure reason text. Then add `Sources/CodeRelayCodex/CodexUsageRefreshService.swift` with a small service API that wraps `CodexUsageFetcher` and returns a typed refresh result for one managed account id and scope. The result must preserve `UsageProbeSource`, `UsageProbeStatus`, and failure metadata that later UI can show, but it must not perform cache persistence, warning thresholds, or switch recommendations.</action>
  <verify>
    <automated>swift test --filter CodexUsageFetcherTests</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCodex/CodexUsageRefreshService.swift` contains a result type with `source` and `status`
- at least one test asserts missing credentials do not synthesize fake percentage values
- at least one test asserts transport or decode failure is represented in the refresh result
- No file in this task adds switch, warning, relaunch, or resume behavior
  </acceptance_criteria>
  <done>The probing layer can now report trustworthy usage data and equally trustworthy failure states.</done>
</task>

</tasks>

<verification>
Run `swift test --filter CodexUsageFetcherTests`. Confirm the Codex usage layer reads only managed-home credentials, normalizes both windows, and preserves error/unknown status instead of hiding failures.
</verification>

<success_criteria>
Phase 2 has a Codex-only usage probing layer that is safe for managed accounts and explicit about source and freshness.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usage-monitoring-readiness/02-02-SUMMARY.md`
</output>
