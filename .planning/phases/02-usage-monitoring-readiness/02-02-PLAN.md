---
phase: 02-usage-monitoring-readiness
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - Sources/CodeRelayCodex/CodexUsageFetcher.swift
  - Sources/CodeRelayCodex/CodexUsageRefreshService.swift
  - Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift
  - Tests/CodeRelayCodexTests/CodexUsageRefreshServiceTests.swift
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04
  - MON-05
must_haves:
  truths:
    - "User can manually refresh a managed account and receive current 5-hour and weekly usage when that account's scoped Codex credentials are valid."
    - "User sees explicit stale, error, or unknown monitoring state after refresh problems instead of the usage view silently clearing."
    - "User can trust that monitoring refresh stays within the Codex-managed account flow and does not introduce warning, switching, relaunch, or resume behavior in Phase 2."
  artifacts:
    - path: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      provides: "Managed-home OAuth usage fetch and window normalization"
    - path: Sources/CodeRelayCodex/CodexUsageRefreshService.swift
      provides: "Typed single-account refresh result with stale/error/unknown handling"
    - path: Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift
      provides: "Managed-home request, auth parsing, and normalization coverage"
    - path: Tests/CodeRelayCodexTests/CodexUsageRefreshServiceTests.swift
      provides: "Refresh-result fallback and failure-state coverage"
  key_links:
    - from: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      to: Sources/CodeRelayCodex/CodexHomeScope.swift
      via: "managed-home auth.json and config.toml resolution"
      pattern: "CodexHomeScope"
    - from: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      to: Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
      via: "normalized monitoring snapshot output"
      pattern: "ManagedAccountUsageSnapshot"
    - from: Sources/CodeRelayCodex/CodexUsageRefreshService.swift
      to: Sources/CodeRelayCodex/CodexUsageFetcher.swift
      via: "refresh orchestration and stale fallback mapping"
      pattern: "CodexUsageFetcher"
---

<objective>
Implement the Codex-specific monitoring probe for Phase 2: managed-home usage fetching plus typed refresh results that preserve truthful stale/error/unknown states.

Purpose: The app cannot render trustworthy monitoring until it can probe a managed account directly and distinguish fresh data from cached fallback or outright failure.
Output: A managed-home usage fetcher and a refresh service that returns typed monitoring results one account at a time.
</objective>

<execution_context>
@/Users/xiachy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xiachy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
@.planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
@Sources/CodeRelayCore/ManagedAccount.swift
@Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
@Sources/CodeRelayCodex/CodexHomeScope.swift
@Sources/CodeRelayCodex/CodexIdentityReader.swift
@../CodexBar/docs/codex.md
@../CodexBar/docs/refresh-loop.md
@../CodexBar/Sources/CodexBarCore/Providers/Codex/CodexOAuth/CodexOAuthUsageFetcher.swift
@../CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift

<interfaces>
From Phase 2 Plan 01 output:
```swift
public enum UsageProbeSource: String, Codable, Sendable {
    case managedHomeOAuth
    case cache
    case unknown
}

public enum UsageProbeStatus: String, Codable, Sendable {
    case fresh
    case stale
    case error
    case unknown
}

public struct ManagedAccountUsageSnapshot: Codable, Equatable, Sendable {
    public let accountID: UUID
    public let fiveHourWindow: RateWindow?
    public let weeklyWindow: RateWindow?
    public let updatedAt: Date
    public let source: UsageProbeSource
    public let status: UsageProbeStatus
    public let lastErrorDescription: String?
}
```

From `Sources/CodeRelayCodex/CodexHomeScope.swift`:
```swift
public struct CodexHomeScope: Equatable, Sendable {
    public let accountID: UUID
    public let homeURL: URL
    public var authFileURL: URL { get }
    public var configFileURL: URL { get }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the managed-home usage fetcher and normalize the API response</name>
  <files>Sources/CodeRelayCodex/CodexUsageFetcher.swift, Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- .planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
- ../CodexBar/docs/codex.md
- ../CodexBar/Sources/CodexBarCore/Providers/Codex/CodexOAuth/CodexOAuthUsageFetcher.swift
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
- Sources/CodeRelayCodex/CodexHomeScope.swift
- Sources/CodeRelayCodex/CodexUsageFetcher.swift
- Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift
  </read_first>
  <behavior>
- the fetcher reads auth data from the managed account's `auth.json`, never from ambient `~/.codex`
- the fetcher maps the managed-home usage response into one 5-hour window and one weekly window
- failures to load credentials, decode the response, or authorize the request are surfaced as typed errors instead of partial values
  </behavior>
  <action>First add failing `Phase2_...` tests to `Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift` for managed-home request construction, `auth.json` token parsing, successful 5-hour and weekly window normalization, missing-token handling, and non-2xx failures. Then create `Sources/CodeRelayCodex/CodexUsageFetcher.swift` with a `CodexUsageFetcher` protocol and a concrete `DefaultCodexUsageFetcher`. Read the access token from `CodexHomeScope.authFileURL`, accepting `tokens.accessToken` or `tokens.access_token`. Read the account header value from `tokens.account_id`, `tokens.accountId`, `workspace_id`, or `workspaceId` when present. Call the exact managed-home OAuth endpoint `GET https://chatgpt.com/backend-api/wham/usage` with `Authorization: Bearer <token>` and `Accept: application/json`. If the scoped `config.toml` contains `chatgpt_base_url`, normalize it the same way CodexBar does before appending `/wham/usage` or `/api/codex/usage`. Map `rate_limit.primary_window` into `fiveHourWindow` and `rate_limit.secondary_window` into `weeklyWindow` using `RateWindow`, then return `ManagedAccountUsageSnapshot(accountID: ..., source: .managedHomeOAuth, status: .fresh, updatedAt: .now, lastErrorDescription: nil)`. Do not read ambient `~/.codex`, launch `codex app-server`, or parse PTY output in this task.</action>
  <verify>
    <automated>swift test --filter Phase2_codexUsageFetcher</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCodex/CodexUsageFetcher.swift` contains `public protocol CodexUsageFetcher`
- `Sources/CodeRelayCodex/CodexUsageFetcher.swift` contains `public struct DefaultCodexUsageFetcher`
- `Sources/CodeRelayCodex/CodexUsageFetcher.swift` contains `backend-api/wham/usage`
- `Sources/CodeRelayCodex/CodexUsageFetcher.swift` contains `Authorization`
- `Tests/CodeRelayCodexTests/CodexUsageFetcherTests.swift` contains `func Phase2_codexUsageFetcher_buildsManagedHomeRequest`
- `rg -n "app-server|TTY|WKWebView|Cookie" Sources/CodeRelayCodex/CodexUsageFetcher.swift` returns no matches
  </acceptance_criteria>
  <done>CodeRelay has one concrete, managed-home-scoped usage probe that returns normalized monitoring snapshots for a managed account.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the refresh service with stale, error, and unknown fallback rules</name>
  <files>Sources/CodeRelayCodex/CodexUsageRefreshService.swift, Tests/CodeRelayCodexTests/CodexUsageRefreshServiceTests.swift</files>
  <read_first>
- .planning/phases/02-usage-monitoring-readiness/02-RESEARCH.md
- .planning/phases/02-usage-monitoring-readiness/02-VALIDATION.md
- ../CodexBar/docs/refresh-loop.md
- ../CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift
- Sources/CodeRelayCore/ManagedAccount.swift
- Sources/CodeRelayCore/ManagedAccountUsageSnapshot.swift
- Sources/CodeRelayCodex/CodexUsageFetcher.swift
- Sources/CodeRelayCodex/CodexUsageRefreshService.swift
- Tests/CodeRelayCodexTests/CodexUsageRefreshServiceTests.swift
  </read_first>
  <behavior>
- a refresh result tells callers whether the returned monitoring state is `fresh`, `stale`, `error`, or `unknown`
- cached monitoring data is preserved and marked `stale` when a fetch fails after a prior successful snapshot exists
- accounts with no credentials or no usable prior snapshot surface `unknown` or `error` instead of fabricated percentages
  </behavior>
  <action>First add failing `Phase2_...` tests to `Tests/CodeRelayCodexTests/CodexUsageRefreshServiceTests.swift` for fresh success, stale fallback from a cached snapshot, `unknown` when no auth data or cached snapshot exists, and `error` when the network or decode path fails without a usable snapshot. Then create `Sources/CodeRelayCodex/CodexUsageRefreshService.swift` with `ManagedAccountUsageRefreshResult`, `CodexUsageRefreshService`, and `DefaultCodexUsageRefreshService`. Use this exact result shape: `accountID: UUID`, `snapshot: ManagedAccountUsageSnapshot?`, `status: UsageProbeStatus`, `source: UsageProbeSource`, and `message: String?`. Implement `refresh(account: ManagedAccount, cachedSnapshot: ManagedAccountUsageSnapshot?) async` with these rules: on fetch success, return the fresh snapshot from `CodexUsageFetcher`; on fetch failure with a cached snapshot, return that snapshot's windows but change the returned snapshot's `source` to `.cache`, `status` to `.stale`, and `lastErrorDescription` plus `message` to the failure text; on missing auth or missing token with no cached snapshot, return `status: .unknown`, `source: .unknown`, and `snapshot: nil`; on server, decode, or transport failure with no cached snapshot, return `status: .error`, `source: .managedHomeOAuth`, and `snapshot: nil`. The refresh service must not write the cache, batch accounts, add warnings, or add switch/relaunch behavior.</action>
  <verify>
    <automated>swift test --filter Phase2_codexUsageRefreshService</automated>
  </verify>
  <acceptance_criteria>
- `Sources/CodeRelayCodex/CodexUsageRefreshService.swift` contains `public struct ManagedAccountUsageRefreshResult`
- `Sources/CodeRelayCodex/CodexUsageRefreshService.swift` contains `public protocol CodexUsageRefreshService`
- `Sources/CodeRelayCodex/CodexUsageRefreshService.swift` contains `func refresh(account: ManagedAccount, cachedSnapshot: ManagedAccountUsageSnapshot?) async`
- `Tests/CodeRelayCodexTests/CodexUsageRefreshServiceTests.swift` contains `func Phase2_codexUsageRefreshService_returnsStaleSnapshotWhenFetchFailsWithCache`
- `Tests/CodeRelayCodexTests/CodexUsageRefreshServiceTests.swift` contains `func Phase2_codexUsageRefreshService_returnsUnknownWhenNoSnapshotExists`
- `rg -n "warning|switch|resume|relaunch" Sources/CodeRelayCodex/CodexUsageRefreshService.swift` returns no matches
  </acceptance_criteria>
  <done>The monitoring probe layer can now return equally trustworthy success, stale, unknown, and error states for a single managed account refresh.</done>
</task>

</tasks>

<verification>
Run `swift test --filter Phase2_codexUsageFetcher` and `swift test --filter Phase2_codexUsageRefreshService`. Confirm the managed-home probe uses scoped credentials, normalizes both windows, and preserves truthful stale/unknown/error outcomes instead of hiding failures.
</verification>

<success_criteria>
Phase 2 has a managed-home Codex monitoring probe and refresh-result layer that the app can call without inventing freshness or readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usage-monitoring-readiness/02-02-SUMMARY.md`
</output>
